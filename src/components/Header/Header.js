import React from "react";
import "./Header.css";

const Header = props => (
  <header className="header">
   
    <h2>
      Try not to click on an image more than once!
    </h2>
  </header>
);

export default Header;
